'use strict';
import { NFC_PREFIX_DEFAULT } from '../../constants';

interface NfcBroadcastModalPropsTypes {
  currency?: number;
  currencyNumberStandart?: number;
  account?: number;
  purpose?: 'sync' | 'sign';
}

const NfcBroadcastModal = async (props: NfcBroadcastModalPropsTypes) => {
  
}
export default NfcBroadcastModal